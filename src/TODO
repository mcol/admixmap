
namespaces for bcppcl

package for tools

check compilation with Intel compiler

--with-baselib-path

--with-bcppcl-path

--with-sprng-path

--with-mpi-path

checks for sprng, gmp, bcppcl

error messages if MPI, sprng, gsl etc no found